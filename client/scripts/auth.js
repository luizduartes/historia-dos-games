function auth_login() {
    let email = ipt_email.value
    let password = ipt_senha.value

    // Logar com a API web-data-viz
    console.log(email)
    console.log(password)
}

function auth_register() {
    let username = ipt_username.value
    let email = ipt_email.value
    let password = ipt_senha.value

    // Cadastrar com a API web-data-viz
    console.log(username)
    console.log(email)
    console.log(password)
}
